# ── Web 框架 ──────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9        # 文件上传（multipart）

# ── 数据访问（SQLAlchemy 2.0 + SQLite 起步）────────────
sqlalchemy>=2.0
pydantic>=2.9
pydantic-settings>=2.5         # .env 配置（Provider 密钥/降级顺序）
python-dotenv>=1.0

# ── 鉴权（JWT + bcrypt）──────────────────────────────
PyJWT>=2.9
passlib[bcrypt]>=1.7.4

# ── 检测/视觉（YOLO26）───────────────────────────────
ultralytics>=8.4.0             # YOLO26 支持（安装时取最新稳定版）
opencv-python>=4.10
numpy>=1.26

# ── 向量库（Milvus Lite 起步，可升级 Standalone）────────
pymilvus[milvus-lite]>=2.5

# ── VLM / HTTP 客户端 ────────────────────────────────
httpx>=0.27

# ── Phase 2 后装（预留，不阻塞 Phase 0/1）──────────────
# open_clip_torch      # 路由 CLIP 粗筛
# bge-m3 / bge-reranker # RAG 文本检索（见文档 §3.2）
# celery[redis]        # 长任务队列（asyncio 不够时升级）
