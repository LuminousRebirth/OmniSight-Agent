# ---- Web framework ----
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9

# ---- Data access (SQLAlchemy 2.0, SQLite first) ----
sqlalchemy>=2.0
pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0

# ---- Auth (JWT + bcrypt) ----
PyJWT>=2.9
passlib[bcrypt]>=1.7.4

# ---- Detection / Vision (YOLO26) ----
ultralytics>=8.4.0
opencv-python>=4.10
numpy>=1.26

# ---- Vector DB (Milvus Lite, upgradeable to Standalone) ----
pymilvus[milvus-lite]>=2.5

# ---- VLM / HTTP client ----
httpx>=0.27

# ---- Phase 2 additions (uncomment when needed) ----
# open_clip_torch
# bge-m3 / bge-reranker
# celery[redis]
